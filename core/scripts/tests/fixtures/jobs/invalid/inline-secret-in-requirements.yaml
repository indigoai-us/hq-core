id: inline-secret-job
name: Inline secret rejected
schedule: "0 9 * * *"
timezone: UTC
runtime: claude
exec:
  prompt: Must not embed credentials in requirements.
timeout_seconds: 600
notify: dm
enabled: true
owner: user@example.com
created_at: "2026-08-23T14:00:00Z"
requirements:
  runtime: claude
  secrets:
    - AKIAIOSFODNN7EXAMPLE
