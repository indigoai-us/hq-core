# HQ Company Manifest
# Single source of truth for company -> resource mapping
# Run /newcompany to add entries, or edit manually.

companies:
  # Empty by default. The opt-in HQ_INDIGO_MCP=1 path in core/scripts/setup.sh
  # and /newcompany append entries here as `<slug>:` blocks at this indent.
