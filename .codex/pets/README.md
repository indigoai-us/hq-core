# Codex Pets

Shared custom Codex pet packages live here for team review and reuse.

To install a pet locally, copy its folder into:

```text
~/.codex/pets/
```

Each pet folder should include `pet.json` and `spritesheet.webp`. Preview images such as `contact-sheet.png` are for review only.
