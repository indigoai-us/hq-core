---
description: Search across HQ and indexed repos using qmd semantic and full-text search
allowed-tools: Read, Grep, Bash(qmd:*), Bash(grep:*), Bash(ls:*)
argument-hint: "\"query\" [--mode search|vsearch|query] [-c collection] [-n count] [--full]"
visibility: public
---

# /search

Read `.claude/skills/search/SKILL.md` and execute that workflow.

Use `$ARGUMENTS` as the search query and flags.
