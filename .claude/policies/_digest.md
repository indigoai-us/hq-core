# Policy Digest — Global (.claude/policies/)

> Auto-generated by `scripts/build-policy-digest.sh`. Do not edit by hand.
> Source: `.claude/policies`
> Policies: 297 (84 hard / 213 soft)

## Hard-enforcement (84)

- [hard] **blog-post-x-draft**: When publishing a new blog article to {your-name}.com (writing MDX to `repos/private/personal-website/src/content/blog/`), always draft an X post to share th...
- [hard] **claude-desktop-bridge-state-zombie**: If Claude desktop shows signs of a memory leak — macOS "out of application memory" dialog, Activity Monitor reporting tens of GB+ for the Claude process, s...
- [hard] **company-archive-cleanup**: When archiving or deleting a company, always:
- [hard] **company-skill-bridge**: Every top-level skill or command authored under a company folder gets a root-level slash-command counterpart, automatically, via symlink. Authors write to th...
- [hard] **deconflict-postbridge-schedule**: Before scheduling a batch of posts via Post-Bridge:
- [hard] **email-humanize**: Before sending or drafting any outbound email (cold outreach, replies, follow-ups), run the content-brand humanize pass:
- [hard] **git-add-explicit-paths-no-drift**: Before committing a focused deliverable (PRD, policy, infrastructure file, feature code), run `git status --short` and inspect the working tree. If unrelated...
- [hard] **git-checkout-not-a-probe**: `git checkout {branch} -- .` is NOT a read-only probe. It overwrites your current working tree with every file from `{branch}`, leaving HEAD pointing at your...
- [hard] **git-filter-repo-case-variants**: When using `git filter-repo --replace-text`, add explicit replacement rules for EVERY case variant of each term (lowercase, Capitalized, UPPERCASE). The tool...
- [hard] **hq-always-pr-shared-state-repos**: Never push commits directly to `main` (or any shared-state branch: `master`, `staging`, `production`, `release/*`) on ANY repo. Always:
- [hard] **hq-askuserquestion-max-4-batch-rounds**: `AskUserQuestion` accepts a maximum of 4 questions per call. When an interview, brainstorm, or plan step lists more than 4 mandatory questions:
- [hard] **hq-auth-middleware-whitelist-password-flow**: ALWAYS: For password-protected app flows where the entry-point endpoints (e.g. `/__access` form, `/api/access/verify` POST) must serve unauthenticated visito... <!-- applies_to: nextauth,clerk -->
- [hard] **hq-bash-discipline**: Use `IFS=$'\t'` (not `$"\t"`); never `IFS=":" read` over paths; no GNU coreutils or Bash 4+ on macOS BSD; rediscover pgrep PIDs and validate with `ps`; use `...
- [hard] **hq-bash-no-gnu-coreutils-date-timeout**: NEVER use GNU-only coreutils features in HQ scripts. macOS ships BSD coreutils by default; the two most common gotchas observed in practice are:
- [hard] **hq-bash-no-ifs-colon-read-path-corruption**: NEVER use `IFS=":" read` (or `while IFS=":" read -r a b c`) inside a shell loop when any field can contain a colon, when any field is itself a filesystem pat...
- [hard] **hq-bash-pgrep-no-hardcode-pids**: NEVER hardcode a PID discovered by `pgrep` (or `ps`) into a follow-up cron/scheduled-task body, state file, or shell variable that survives across orchestrat...
- [hard] **hq-bash-pgrep-self-match-validate-with-ps**: NEVER trust a `pgrep -f <pattern>` result without a second-stage `ps` validation. `pgrep -f` matches the FULL command line of every process, including the ba...
- [hard] **hq-bash-portable-no-bash4**: NEVER use `mapfile`, `readarray`, associative arrays (`declare -A`), `${var,,}`/`${var^^}` case-conversion, or any other Bash 4+ builtin in HQ scripts. macOS...
- [hard] **hq-bash-set-e-status-returns**: When a bash function intentionally returns non-zero exit codes as status signals (e.g., `handle_failure` returning 2=skip, 3=pause), NEVER call it directly u...
- [hard] **hq-cmd-run-project-ralph-hard-pause-procedure**: When hard-pausing `/run-project --ralph-mode`, execute ALL FIVE steps in order. Skipping any step leaves zombie state that blocks the next launch.
- [hard] **hq-core-protect-bypass-inline**: The `HQ_BYPASS_CORE_PROTECT=1` env var must be passed inline with the command that modifies the file (e.g., `HQ_BYPASS_CORE_PROTECT=1 sed -i '' '...' .claude...
- [hard] **hq-cross-repo-privilege-tier-surface-scope**: Before implementing any new identity tier, role, group, or privilege primitive that will be referenced from more than one repo, STOP and surface to the user:
- [hard] **hq-deploy-reinforcement**: `/deploy` (the hq-deploy skill) is the default and reinforced sharing path for any HQ artifact that has a URL form. Prefer it over ad-hoc Vercel, Netlify, Gi...
- [hard] **hq-env-file-no-trailing-newline**: A `.env*` file MUST NOT contain a line of the form `KEY="…\n"` or `KEY="… "` (trailing space / tab / literal `\n` inside the quoted value).
- [hard] **hq-git-branch-verify**: ALWAYS run `git branch --show-current` before committing to any repo. Never assume the current branch — inherited cwd or installs can silently land you on ...
- [hard] **hq-git-discipline**: Verify branch + pull before commit; explicit paths (no `git add -A` with drift); `git checkout -- .` is destructive (not a probe); never push HQ; preserve WI...
- [hard] **hq-git-log-shell-wrapper-hides-merges**: Two distinct `git log` failure modes can hide merge commits from display. NEITHER `git log --oneline` NOR `git log --graph` is a reliable source of truth for...
- [hard] **hq-git-push-refspec-chip-safe**: When committing to a specific branch from a worktree that may be hijacked by spawned-task chips, use detached-HEAD + push refspec in a single bash invocation:
- [hard] **hq-git-reflog-expire-all-destroys-stash**: ALWAYS preserve at-risk WIP before any reflog expiration or garbage collection that drops unreachable objects. Specifically:
- [hard] **hq-git-staged-deletion-verify-blob-before-reset**: When `git status` shows the confusing pair:
- [hard] **hq-git-stash-for-focused-pr-with-wip**: ALWAYS use `git stash push -u -m "<label>"` to capture both modified AND untracked files when you need to land a focused PR while uncommitted WIP exists in t...
- [hard] **hq-glob-scoped-path**: ALWAYS pass `path:` to Glob scoped to a subdirectory (e.g. `projects/`, `workers/`). Glob from HQ root times out (`.ignore` doesn't protect it). Grep from HQ...
- [hard] **hq-handoff-changeset-scope**: When the HQ root has a large untracked baseline, NEVER treat whole-repo `git status` output as the session scope. The durable scope for `/handoff` is the ses...
- [hard] **hq-image-context-isolation**: The parent session must never accumulate more than ~10 images in its context window. When reading, verifying, or analyzing image files, delegate to a sub-agent.
- [hard] **hq-mcp-absolute-paths**: ALWAYS use absolute paths (e.g. `/opt/homebrew/bin/npx`, `/opt/homebrew/bin/node`) for `command` fields in `.mcp.json` stdio-type servers. Bare `npx` or `nod...
- [hard] **hq-never-echo-tokens-stdout**: NEVER print raw API keys, tokens, or secrets to stdout in CLI setup/config commands. Use `<your-api-key>` or `<paste-your-key>` placeholders instead. CLI set...
- [hard] **hq-no-force-push-diverged-release-branch**: NEVER force-push a release branch (e.g. `release/v11.x.x`), `main`, `master`, or any production branch when `git fetch` shows origin has commits you don't ha...
- [hard] **hq-no-grep-discovery**: Never use Grep to find `prd.json`, `worker.yaml`, or discover project/company/worker directories. This is hook-enforced — Grep calls with these patterns ar...
- [hard] **hq-no-parent-import-from-child-component**: A child component MUST NOT import a module from its parent shell (the component that renders it). That creates a circular import graph — `Parent.tsx → Ch...
- [hard] **hq-no-production-testing**: 1. NEVER use production API endpoints, credentials, or accounts for testing, debugging, or development. Always ask which environment to target. If no staging...
- [hard] **hq-no-test-shortcuts**: Always fix tests properly — never skip flaky tests, never add `test.skip` as a workaround, never create false positives. When a test fails, investigate roo...
- [hard] **hq-no-worktree-for-repo-work**: NEVER create a git worktree or new branch in HQ when starting work on a project repo (VYG, {company}-{your-project}, etc.). HQ must stay on `main` at all tim...
- [hard] **hq-oidc-access-denied-diagnose-via-cloudtrail**: When `AssumeRoleWithWebIdentity` returns `AccessDenied`, do NOT start by re-reading the IAM trust policy JSON. It almost always looks syntactically correct �...
- [hard] **hq-oidc-migration-plan-both-subject-shapes**: Before cutting any workflow over from IAM-user access keys to OIDC federation, inventory ALL jobs that will assume the target role and classify each by subje...
- [hard] **hq-orchestrator-lockfile-sync**: After all stories complete in run-project.sh, run `npm install` (or equivalent package manager install) in the project repo and commit the updated lockfile b...
- [hard] **hq-permission-simple-expansion-extract-to-script**: NEVER try to defeat Claude Code's `simple_expansion` permission gate by adding more prefix allow rules (`Bash(nohup:*)`, `Bash(bash -c*:*)`, wildcard verb ru...
- [hard] **hq-permissions-fan-out-edit-write-multiedit**: Claude Code evaluates `(tool, pattern)` tuples independently. An entry like `Edit(workspace/threads/)` authorizes Edit only — it does NOT authorize Write o...
- [hard] **hq-pr-single-concern**: 1. Every PR must address exactly one concern. A concern is: one bug fix, one feature, one refactor, one infrastructure change, or one dependency update. If y...
- [hard] **hq-prd-no-implement**: When `/plan` is invoked, the ONLY outputs are:
- [hard] **hq-publish-pipeline-two-stop**: ALWAYS treat HQ publish as a two-stop pipeline:
- [hard] **hq-rm-permission-allow-scope-paths**: Never add `Bash(rm:*)`, `Bash(rm -rf:*)`, or any unrestricted `rm` wildcard to the `permissions.allow` list in `.claude/settings.json` or `.claude/settings.l...
- [hard] **hq-run-project-repo-bootstrap**: Before launching `run-project.sh`, verify the PRD's `metadata.repoPath` exists as a git repo with at least one commit. The script uses git worktrees for bran...
- [hard] **hq-run-project-swarm-branch-validation**: Before `run-project.sh` marks a story `completed` in swarm mode, the orchestrator MUST verify the story's merge commit lives on `{prd.branchName}` and NOT on...
- [hard] **hq-run-project-swarm-merge-conflict-tombstone**: When `scripts/run-project.sh --swarm` merges a per-story worktree branch into the shared feature branch and the merge fails (conflict, hook failure, aborted ...
- [hard] **hq-settings-local-for-personal-allows**: `.claude/settings.json` (HQ root) is core.yaml-locked and is the committed public kernel — it must not accumulate personal allow entries, absolute user pat...
- [hard] **hq-slack-verify-scopes-beyond-auth-test**: NEVER trust Slack API "successful" responses at face value when a bot token has limited scopes. `auth.test` succeeding only proves the token is valid and tie... <!-- applies_to: slack -->
- [hard] **hq-subagent-no-mcp**: Sub-agents spawned via Task() don't inherit MCP server connections. Workers needing external tools (Codex, etc.) must use CLI via Bash, not MCP tools declare...
- [hard] **hq-swarm-pr-branch**: After `run-project --swarm` completes, all story commits are cherry-picked onto local `main` (the working tree), NOT the PRD's `branchName`. To create a PR:
- [hard] **hq-user-specified-tool-unavailable**: When the user explicitly instructs you to use a specific tool (e.g. "use Paper MCP", "use the Slack MCP", "use Playwright") and that tool is not available (n...
- [hard] **hq-vercel-discipline**: Eleven hard rules for Vercel operations. Each preserves the failure mode and remedy of its source slug (see `merged_from`). Soft-enforcement Vercel policies ... <!-- applies_to: vercel -->
- [hard] **hq-vercel-wildcard-single-subdomain-level**: Vercel wildcard custom domains (`*.example.com`) match EXACTLY ONE subdomain level. A host like `preview.{slug}.example.com` (two labels under `example.com`)...
- [hard] **linear-scan-check-existing-prds**: Before recommending a new PRD from a Linear scan, always check `companies/{product}/projects/` for existing PRDs that cover the same Linear issues. Use `ls c... <!-- applies_to: linear -->
- [hard] **mcp-process-cleanup**: MCP servers spawned via stdio (npx/tsx) leak as orphaned processes when Claude sessions end. The `cleanup-mcp-processes` Stop hook kills these on session exi...
- [hard] **mcp-transport-detection**: Before adding a new MCP server to `.mcp.json`, test the transport protocol. Not all HTTP MCP servers accept plain `"type": "http"` (JSON POST). Servers using...
- [hard] **model-routing-opus-only**: All Claude work runs on Opus 4.6. Never use Sonnet.
- [hard] **no-headless-browser-in-vercel-lambda**: NEVER run Playwright, Puppeteer, or Chromium in a Vercel Lambda. Use ingest-only endpoints that accept pre-captured payloads from client-side callers (extens...
- [hard] **no-relative-symlinks-from-worktree**: NEVER use relative symlinks to access pattern-2 knowledge repos from a git worktree. `../../repos/` resolves against the worktree root, not the HQ root. Use ...
- [hard] **no-shared-skill-extraction-touching-5-files**: NEVER extract a shared skill that requires editing 5+ existing files to wire up. When extending behavior across multiple commands/skills, prefer layered inde...
- [hard] **no-threaded-posts**: Never generate, suggest, queue, or post content with type "thread" (multi-part X threads like 1/5, 2/5, etc.).
- [hard] **post-bridge-media-upload**: When scheduling posts in Post-Bridge with images:
- [hard] **post-bridge-media-workflow**: When scheduling posts in Post-Bridge, images cannot be added inline. Use the 2-step flow:
- [hard] **post-bridge-unicode-payload**: NEVER shell-expand JSON payloads containing unicode characters (em dashes —, curly quotes, etc.) for Post-Bridge API calls. The shell's `$(...)` expansion ...
- [hard] **prd-json-schema**: When creating `prd.json` files:
- [hard] **prd-json-validation-post-task**: After any sub-agent writes to prd.json (setting `passes`, adding `notes`, updating `files`), validate the JSON is parseable before proceeding to the next sto...
- [hard] **prd-no-execute**: 1. After `/prd` creates project files (`prd.json` + `README.md`), run `/handoff` and end the session
- [hard] **prd-userstories-key**: PRDs MUST use `"userStories"` as the array key for stories (not `"stories"`). Every story object MUST include `"passes": false`. `run-project.sh` validates b...
- [hard] **prefer-systemic-fix-over-user-bandaid**: When a bug affects multiple users — or could affect them, because the cause is structural (wrong default, version drift, env mismatch, stale published pack...
- [hard] **publish-kit-source-is-strict-allowlist**: publish-kit MUST treat its source scope as a strict allowlist of HQ-core paths. The walker never traverses owner-private top-level directories. The target di...
- [hard] **run-project-conflict-marker-guard**: `validate_git_state()` in `scripts/run-project.sh` (and every swarm-merge auto-commit path) MUST scan the staged diff for unresolved merge-conflict markers b...
- [hard] **run-project-local-keyword**: Never use `local` keyword in the top-level swarm or sequential mode loop bodies of `run-project.sh`. These loops are NOT inside functions — `local` only wo...
- [hard] **run-project-monitor-spawn-keystroke-race**: The `spawn_cmux_monitor` routine in `scripts/run-project.sh` must launch the monitor window via a temporary `.command` file opened with `open -a Terminal`. I...
- [hard] **run-project-name-matches-dir**: When invoking `scripts/run-project.sh` or `/run-project`, the project argument must exactly match the directory name under `companies/*/projects/{name}/`. Do...
- [hard] **run-project-no-permissions-required**: ALWAYS pass `--no-permissions` when launching `run-project.sh` for an unattended (non-interactive) orchestrator run.
- [hard] **run-project-worktree-heal-orphan**: `ensure_worktree` in `scripts/run-project.sh` must detect and heal orphan directories at the target worktree path BEFORE calling `git worktree add`. `git wor...

## Soft-enforcement (213)

- [soft] **agent-browser-react-false-positives**: When running E2E tests with agent-browser against data-dependent pages:
- [soft] **anthropic-pricing-verify**: Always web-search current Anthropic pricing before hardcoding cost constants. Pricing changes frequently — GoClaw had stale rates (haiku $0.80 vs actual $0... <!-- applies_to: anthropic -->
- [soft] **ascii-art-character-verify**: When manually composing ANSI Shadow block art, ALWAYS verify each character against the skill's character map before screenshotting. Characters T (`╚══...
- [soft] **auto-deploy-on-create**: When HQ creates or builds a web-servable artifact, automatically deploy it to hq-deploy and present the live URL. No user prompt needed — deploy is a defau...
- [soft] **bulk-sed-exception-ordering**: When doing a bulk `find + sed` sweep across many files with specific exceptions, ALWAYS:
- [soft] **chrome-mcp-multi-page-audit**: When auditing multiple pages via Chrome MCP:
- [soft] **chunked-reads-large-files**: For files over 500 LOC, use the `offset` and `limit` parameters to read in sequential chunks. Never assume a single Read captured the complete file — the R...
- [soft] **cio-browser-navigation**: When navigating Customer.io via agent-browser:
- [soft] **clipboard-file-protocol**: `navigator.clipboard.writeText()` requires a secure context (HTTPS). Pages opened via `file://` protocol will always reject the Clipboard API. When building ...
- [soft] **curious-minds-is-book**: `knowledge/public/curious-minds/` is a book (concept reference), NOT company knowledge docs. Skip it during:
- [soft] **dual-codex-review-pattern**: For high-value PRs (10+ stories, security-sensitive code, new feature areas), run both a Claude-based review and `codex review --base {branch}` (gpt-5.4). Th...
- [soft] **dual-repo-prd-routing**: When a PRD spans two repos (`repoPath` + `secondaryRepoPath`), add a `REPO:` prefix in each story's `notes` field directing the sub-agent to the correct repo...
- [soft] **figma-icon-export**: Figma MCP screenshots include canvas background and cannot be saved as files. For app icon work, have the user export the icon from Figma as a high-res PNG, ...
- [soft] **gmail-token-refresh**: Before sending emails via Gmail MCP, test token validity with a lightweight call first (e.g., `mcp__gmail__list_emails` with limit 1). If it fails with `inva... <!-- applies_to: gmail -->
- [soft] **google-fonts-satori**: When loading fonts for `next/og` (Satori), never hardcode Google Fonts gstatic URLs — they change with font versions (e.g., v22 → v24). Instead, fetch th...
- [soft] **gstack-prefix-mapping**: gstack skills are installed with a `g-` prefix. Their SKILL.md prose references sibling
- [soft] **hook-macos-case-paths**: When writing bash hooks that compare file paths (e.g. checking if a path is inside `repos/`), always use case-insensitive matching or pattern-based checks (e...
- [soft] **hq-alert-baseline-calibration**: 1. Before setting alert thresholds, document the expected baseline behavior including known patterns that should NOT trigger alerts. If 51% of a certain even...
- [soft] **hq-announce-before-irreversible**: 1. Before executing any irreversible action, state what you are about to do, which account/project/environment it targets, and ask for explicit confirmation....
- [soft] **hq-askuserquestion-free-form-is-novel-option**: When the user answers an `AskUserQuestion` with free-form text that does not select any of the presented options, treat the text as a legitimate novel fourth...
- [soft] **hq-astro-verify-hashed-css-bundle**: When verifying CSS tokens, keyframes, custom properties, or any global stylesheet content on a deployed Astro page, fetch the hashed `/_astro/*.css` bundle (...
- [soft] **hq-aws**: Consolidated AWS platform guardrails covering identity (Cognito, IAM), compute (Lambda), CLI invocation patterns, storage/secrets (Secrets Manager, S3 leak h... <!-- applies_to: aws -->
- [soft] **hq-bash-set-a-source-env-before-subprocess-heredoc**: ALWAYS wrap a dotenv `source` in `set -a` / `set +a` when the next command is a subprocess heredoc that depends on those variables:
- [soft] **hq-bugfix-requires-tests**: When fixing a bug or broken behavior, always add tests or E2E coverage that would catch the regression if it recurred. If unsure about test type or scope (un...
- [soft] **hq-classifier-own-labels-single-source**: When a classifier (tiering, bucketing, status, cohort assignment) is computed in the data layer, the data layer must own the labels too. Never let a display ...
- [soft] **hq-claude-cli-prefill-flag-for-scripted-terminal**: ALWAYS: When you need to pre-populate the `claude` CLI's input box in a scripted Terminal launch, use the hidden `--prefill '<text>'` flag rather than AppleS...
- [soft] **hq-cli-version-read-from-package-json**: ALWAYS: When publishing a CLI to npm, read the version from `package.json` at runtime — never hardcode a version string in source.
- [soft] **hq-cloudformation-sub-for-template-strings**: ALWAYS wrap CloudFormation strings containing `${Var}` template references in `!Sub`. Plain multiline YAML scalars (`>` or `|`) do NOT interpolate — the `$... <!-- applies_to: aws -->
- [soft] **hq-cluster-test-failures-by-root-cause**: NEVER conflate "failing tests" with "one root cause." When investigating test failures:
- [soft] **hq-cmd-handoff-execution-integrity**: `/handoff` is the LAST thing a session does. It must survive the worst-case context state, complete every step, and never destroy the artifact it just produc...
- [soft] **hq-cmd-handoff-must-complete**: When the user requests `/handoff`, the skill MUST complete every step without fail. Never report handoff as done while any step is incomplete.
- [soft] **hq-cmd-handoff-no-discovery-rerun**: NEVER invoke `scripts/handoff-finalize.sh` a second time (synchronously or in a backgrounded subshell) for the sole purpose of "discovering" the thread file ...
- [soft] **hq-cmd-publish-kit-python-yaml-free**: Any Python helper invoked by `/publish-kit` (denylist scrub, policy strip, verification) MUST NOT `import yaml`. macOS system Python 3 ships without the PyYA...
- [soft] **hq-cmd-publish-kit-rerun-diff-on-scope-narrow**: When a patch-release plan is narrowed mid-flow (user excludes files, adds files, or changes the stated scope), the driver MUST re-run the scoped diff (`diff ...
- [soft] **hq-cmd-run-project-pid-tracking**: When monitoring `run-project.sh` execution, NEVER rely solely on PID liveness from check-in status. The orchestrator tracks the shell wrapper PID (`bash -c "...
- [soft] **hq-cmd-run-project-process-cleanup**: Before launching `run-project.sh` (especially with `--resume`), always check for existing orchestrator and sub-agent processes:
- [soft] **hq-cmd-run-project-swarm-orphan-recovery**: When `run-project.sh` swarm mode reports "passes not set" despite a sub-agent exiting with code 0, do NOT assume the work was lost. The sub-agent's commits m...
- [soft] **hq-cmd-stage-kit-settings-json-direct-edit**: `/stage-kit`'s Path Remapping table has no row for `.claude/settings.json` or `.claude/settings.local.json`. Attempting `/stage-kit --item .claude/settings.j...
- [soft] **hq-codex-decision-gate-fallback**: When a Claude-origin HQ command or skill requires a user decision through `AskUserQuestion`, the Codex adaptation must preserve the decision gate even if Cod...
- [soft] **hq-codex-sdk-config-vs-typed-fields**: When wrapping `@openai/codex-sdk`, follow this two-tier routing for any option you want to forward to the spawned `codex` CLI subprocess:
- [soft] **hq-command-output-frontmatter-flag-contract**: When one HQ command's output feeds another command, ALWAYS use a frontmatter flag + file-mtime staleness check as the handshake contract. Do NOT introduce a ...
- [soft] **hq-compiled-ts-rebuild-after-src-edits**: When updating any runtime value (model fallback, default URL, version constant, feature flag default) in a compiled TypeScript package, follow this exact seq...
- [soft] **hq-compute-checksums-path**: The checksum script for HQ Core Governance is at `template/scripts/compute-checksums.sh` (not `scripts/compute-checksums.sh`). Requires `yq` (`brew install y...
- [soft] **hq-confirm-creative-direction**: 1. Before building any creative deliverable, present the intended direction and get explicit approval. Describe the visual style, color palette, tone, layout...
- [soft] **hq-confirm-session-scope-after-plan-approval**: ALWAYS confirm the user's per-session execution scope before starting work when an approved plan meets ANY of:
- [soft] **hq-console-auth-spread-conditional-optional-creds**: When a provider config in hq-console's `auth.ts` (or any Auth.js `NextAuthConfig`) must adapt based on whether an optional credential is present — e.g. PKC...
- [soft] **hq-console-smoke-redirect-is-success**: Post-deploy smoke tests against hq-console MUST classify response codes as follows: <!-- applies_to: vercel -->
- [soft] **hq-cross-session-stage-not-commit**: When a parallel session needs to hand work to a driver session (e.g. policy drafts, staged code edits, bundled-release contributions), the parallel session S...
- [soft] **hq-data-collection-isolation**: NEVER pull high-volume monitoring data (metrics, health checks, dashboards) inline in the parent session — each tick burns 50K+ tokens of tool results.
- [soft] **hq-db-query-probe-real-table**: ALWAYS: When shipping a new DB query pattern (new Scan/Query shape, new index usage, new WHERE clause), probe the real table during development before mergin...
- [soft] **hq-ddb-scan-no-limit-with-filter**: NEVER: Use DynamoDB `Scan` with both `Limit` and `FilterExpression` on a hot path. `Limit` caps the pre-filter row count, not post-filter matches — if the ... <!-- applies_to: aws -->
- [soft] **hq-debug-multi-hop-exhaust-all-layers**: ALWAYS: When debugging a multi-hop end-to-end flow that's broken (e.g. browser → CDN → app → upstream API → DB), don't stop after fixing the first er...
- [soft] **hq-deploy-default-style-goclaw-admin**: When HQ wraps a raw `.html` / `.md` artifact for auto-deploy (deploy skill Step 2.5) or scaffolds a new repo with a web target via `/plan` or `/newworker`, t...
- [soft] **hq-destructive-scripts-default-dry-run**: ALWAYS design destructive-write scripts (CRM mutations, DB upserts/deletes, KV overwrites, Vercel env changes, bulk API PATCH/POST) to default to dry-run mod...
- [soft] **hq-detect-secrets-echo-escape-testing**: NEVER rely on `echo "Bearer <token>"`, `echo "sk-..."`, or similar `echo <secret>` commands as a positive test for `.claude/hooks/detect-secrets.sh`.
- [soft] **hq-distributed-join-partial-failure-diagnosis**: When a distributed join shows "some events match, others don't" (e.g. Install/Activated join successfully while Lead joins zero), suspect visitor-id divergen...
- [soft] **hq-dns-wildcard-shadows-deleted-subdomain**: ALWAYS: After deleting a specific A or CNAME record (e.g. `host.zone.com`) from a zone that also contains a wildcard (`*.zone.com`), remember that the wildca...
- [soft] **hq-docker-build-platform-amd64**: ALWAYS use `docker buildx build --platform linux/amd64` when building images for ECS/EC2 deployment. Never use plain `docker build` on Apple Silicon Macs for...
- [soft] **hq-docker-in-docker-path-translation**: When a container spawns sibling containers via the mounted Docker socket (`/var/run/docker.sock`), bind-mount source paths must use the outer host filesystem...
- [soft] **hq-eas-expo**: Consolidated rules for working with Expo apps and EAS (Expo Application Services) builds, submits, and runtime behavior. Covers submission semantics, transie... <!-- applies_to: expo -->
- [soft] **hq-email-ascii-subjects**: NEVER use special characters (em dash, curly quotes, Unicode punctuation) in email subject lines — they encode as garbled text. Plain ASCII only: hyphens n...
- [soft] **hq-env-local-prod-test-cred-mismatch-check**: ALWAYS verify the token prefixes in a repo's `.env.local` before using it as the credential source for a prod snapshot, migration, or cross-provider cutover.... <!-- applies_to: clerk,stripe,supabase,vercel -->
- [soft] **hq-eslint-allow-default-project-for-root-configs**: NEVER trust local `npm run lint` (or equivalent) as the final gate after adding a new root-level TypeScript config file. CI runs fresh with no cache and will...
- [soft] **hq-figma-token-account-scope**: Figma personal access tokens are account-scoped — they only grant read access to files that the token's owning account owns or has been explicitly shared w...
- [soft] **hq-fix-root-cause-not-symptoms**: NEVER fix a bug by hiding or masking the error. When investigating a failure, always trace to the root cause and fix the actual problem — not just improve ...
- [soft] **hq-ggshield-recursive-for-dirs**: ALWAYS: When invoking `ggshield secret scan path <target>` on a directory, pass `--recursive --yes`. Plain `ggshield secret scan path <dir>` exits with `is a...
- [soft] **hq-git-branch-delete-reverify-current**: Before running `git branch -D <branch>` (or `-d`) in a repo with worktrees, re-run `git branch --show-current` in the target working tree immediately before ...
- [soft] **hq-git-diff-three-dot-for-pr-review**: ALWAYS use three-dot (`A...B`) when diffing a branch against its base to see the true PR delta:
- [soft] **hq-git-divergence-check-both-directions**: NEVER treat `git log A..B` one-sided output as the complete divergence picture. Always check both directions, or use the symmetric form explicitly.
- [soft] **hq-git-fsck-stash-recovery**: ALWAYS recover hook-dropped stashes through dangling-commit archaeology, not blind `git stash apply`:
- [soft] **hq-git-large-diff-audit-before-panic**: ALWAYS decompose a suspiciously-large diff by change type before assuming the release is out of control. Run:
- [soft] **hq-git-merge-ff-only-trunk**: ALWAYS use `git merge --ff-only` when merging a feature branch into a trunk (main, master, release/*) from the command line. `--ff-only` fails loudly on dive...
- [soft] **hq-git-server-side-push-multi-phase-migration**: For multi-phase migration runs that touch a working tree with concurrent unrelated dirty files (other companies, other features, sibling worktrees), promote ...
- [soft] **hq-git-squash-merge-branch-ahead-expected**: ALWAYS: After a squash-merge completes on GitHub (or any remote that squashes), treat a local feature branch reporting "N commits ahead of origin/main" as ex...
- [soft] **hq-git-stage-then-reset-submodule-pointer**: ALWAYS: When staging a worktree where you want most changes but NOT the submodule pointer bump (e.g. an embedded knowledge `.git` repo whose advancement belo...
- [soft] **hq-git-stash-build-artifacts-conflict**: NEVER use `git stash && <command> && git stash pop` to compare tool output before/after changes if the repo has tracked build artifacts (`tsconfig.tsbuildinf...
- [soft] **hq-git-verify-ancestry-before-claiming-on-main**: ALWAYS verify a commit's ancestry against `main` before treating it as "merged" or "on main." Use:
- [soft] **hq-github**: Consolidated rules for working with github.com — the gh CLI, REST/GraphQL APIs, PR/review workflows, Actions, repo state flags, bot credentials, and librar... <!-- applies_to: github -->
- [soft] **hq-github-app-over-pat-for-bot-repo-creation**: ALWAYS prefer a scoped GitHub App (installed on a single organization) over a fine-grained PAT when a bot or automation needs repo-creation scope, org-admin ...
- [soft] **hq-gitignore-before-first-commit**: ALWAYS create `.gitignore` (with `node_modules/`, `.next/`, `.vercel/`, build artifacts) BEFORE running `git init && git add -A && git commit`. If build arti...
- [soft] **hq-gmail-metadata-vs-readonly-scope**: ALWAYS: When choosing Gmail API scopes, prefer `https://www.googleapis.com/auth/gmail.metadata` (headers only: From / To / Cc / Date / Subject — no body, n... <!-- applies_to: gmail -->
- [soft] **hq-grep-preflight-overloaded-compound-pattern**: When grep-verifying that a named section has been removed, NEVER search for the bare name of the section if that name is reused elsewhere in the file for a d...
- [soft] **hq-html-target-blank-noopener**: ALWAYS: Pair `target="_blank"` with `rel="noopener"` on every external `<a>` in HTML decks, sites, and templates:
- [soft] **hq-installer-tauri-capabilities-artifact-isolation**: NEVER: Include auto-generated Tauri manifest artifacts — most notably `src-tauri/gen/schemas/capabilities.json` — in a feature PR whose logical scope is ...
- [soft] **hq-jq-argjson-validate-json-or-default**: ALWAYS: `jq --argjson` requires valid JSON — empty strings cause `invalid JSON text passed to --argjson`. Wrap any JSON assembled from a helper function th...
- [soft] **hq-jq-atomic-edits-large-json-configs**: Use `jq` for atomic structural edits to large JSON config files. The Edit tool requires reading the file first, which fails over the ~25K token cap and preve...
- [soft] **hq-jq-slurpfile-not-argjson-for-large-aggregates**: NEVER: Pass large aggregates to `jq --argjson` on the command line. macOS `ARG_MAX` (~256KB) blows up on real-filesystem scans with:
- [soft] **hq-jq-with-entries-capture-key-variable**: ALWAYS: In `jq`, capture the entry key as a variable before piping into another array filter:
- [soft] **hq-jsdom-vitest-storage-shim**: ALWAYS install Map-backed Storage shims for `localStorage` and `sessionStorage` in vitest test setup when running on jsdom 28. Use the same `Object.definePro...
- [soft] **hq-linear**: Consolidated rules for working with Linear — covers authentication, GraphQL field semantics, batched mutations, and PRD sync hygiene. Sub-rules below prese... <!-- applies_to: linear -->
- [soft] **hq-macos-tar-exclude-appledouble**: ALWAYS: When tarring a directory on macOS for upload to S3 or any non-macOS consumer, exclude AppleDouble metadata files. Use one of:
- [soft] **hq-mcp-claude-in-chrome-no-top-level-await**: The Claude-in-Chrome MCP `javascript_tool` evaluates the supplied code inside a non-async function scope. Top-level `await` throws:
- [soft] **hq-mcp-trial-integration-pattern**: Five structural invariants govern every MCP-server trial in HQ. Apply all five together — they compose into the reusable playbook that mirrors the hook-tri...
- [soft] **hq-migration-bidirectional-grep-verify**: When verifying a brand or content migration in production, run BOTH directions of evidence and require BOTH to pass:
- [soft] **hq-migration-independent-grep-verify**: NEVER trust a migration plan's named-file list as proof of completeness. Before reporting a code migration done, run an independent repo-wide grep for the OL...
- [soft] **hq-migration-phase-boundary-regression-gate**: For migration stories that span multiple infrastructure layers (filesystem rename → infra-as-code update → brand/copy sweep → DNS/redirect cutover), pl...
- [soft] **hq-nested-repo-git-status-check**: `repos/private/hq-core-staging/` is a separate git repository, not a submodule. HQ has no `.gitmodules` file — nested repos are opaque to HQ root git. Afte...
- [soft] **hq-never-fabricate-research-artifacts**: NEVER write a research/discovery artifact on behalf of a command that did not actually execute the underlying work. The producer map must match reality — e...
- [soft] **hq-never-swallow-errors**: 1. NEVER write a catch block that returns null, undefined, empty array, or a success status without logging the error. Every catch block must either re-throw...
- [soft] **hq-nextjs-better-sqlite3-global-singleton**: When a Next.js app uses `better-sqlite3` (or any SQLite adapter with native bindings), the database adapter MUST be a module-level singleton cached on `globa...
- [soft] **hq-nextjs-clean-types-after-page-delete**: After deleting a Next.js page file, run `rm -rf .next` before `tsc --noEmit` or `pnpm typecheck`. The `.next/types/validator.ts` cache retains references to ...
- [soft] **hq-nextjs-env-local-empty-shell-var-precedence**: ALWAYS: When a Next.js route throws a provider auth error ("x-api-key header is required", "No API key provided", etc.) despite the key being present in `.en...
- [soft] **hq-nextjs-host-redirect-requires-domain-attachment**: ALWAYS: For a Next.js `has: [{type: host, value: "sub.example.com"}]` redirect to fire, the host MUST be attached as a domain to the same Vercel project that... <!-- applies_to: vercel -->
- [soft] **hq-nextjs-loading-tsx-over-client-spinner**: For Next.js App Router perceived-latency fixes, prefer colocated `loading.tsx` (automatic `<Suspense>` boundary wrapped around the segment) over wiring custo...
- [soft] **hq-nextjs-navigator-ssr-guard-node24**: NEVER use `typeof navigator !== "undefined"` as an SSR / client-only guard in Next.js / React client components. Starting in Node 24, `globalThis.navigator` ...
- [soft] **hq-nextjs-pin-node-engines-nvmrc**: ALWAYS pin the Node version on every Next.js / Vercel app. Both files must be set and must agree: <!-- applies_to: vercel -->
- [soft] **hq-no-diff-q-in-parallel-bash**: NEVER: Issue `diff -q` (or any command whose non-match exit is nonzero) in a parallel bash batch alongside sibling commands you need to complete. When one to...
- [soft] **hq-no-limits-means-kill-switch**: ALWAYS translate "no limits" into a reactive kill-switch, not zero protection.
- [soft] **hq-nodejs-promisify-scrypt-options-wrap-manual**: NEVER: Call `promisify(scrypt)(password, salt, keylen, options)` in TypeScript. The `util.promisify` type inference only sees the 3-arg overload `scrypt(pass...
- [soft] **hq-npm-subpackage-hydration**: When a repo has a sub-package (e.g. `cli/`, `lambda/`, `worker/`) with its own `package.json` and the root test suite or build invokes binaries from that sub...
- [soft] **hq-npm-version-transitive-check**: NEVER: Assume a newly-published npm version is installable just because `npm publish` succeeded or `npm view <pkg> version` shows it. Transitive dependencies...
- [soft] **hq-octokit-commit-vs-pr-phases-distinct**: Do NOT describe Octokit git data API flows as "atomic" end-to-end. The operation has two distinct phases, and only the first is atomic:
- [soft] **hq-og-images-required**: Every site deployed to Vercel (or any public hosting) MUST have Open Graph images configured before the first production deploy. This includes:
- [soft] **hq-orthogonal-filters-over-overlapping-presets**: When designing filters for a prioritization UI (rep accounts, leads, tasks, tickets), prefer orthogonal filter axes over N preset views that all derive from ...
- [soft] **hq-pandoc-chrome-headless-markdown-to-pdf**: ALWAYS: For markdown → PDF without LaTeX, use pandoc → HTML → Chrome headless (`--headless --print-to-pdf`).
- [soft] **hq-pandoc-chrome-single-page-financial-css**: ALWAYS: When a user asks for a "single-page" financial summary rendered via the pandoc → Chrome pipeline, start with these CSS baselines:
- [soft] **hq-paper-mcp-sequential-agents**: When spawning sub-agents for Paper MCP design work, agents MUST run sequentially — never in parallel. Paper operates on a single shared canvas; two agents ...
- [soft] **hq-permission-rules-literal-subcommand-prefixes**: When pre-approving multi-subcommand CLIs (`git`, `gh`, `npm`, `pnpm`, `vercel`, `docker`, etc.) in the `permissions.allow` list, list each subcommand prefix ...
- [soft] **hq-plan-combined-story-edit-locality**: When the user says "create one combined, minimum story plan" (or any phrasing that asks for a single consolidated story set) after a `/brainstorm` has produc...
- [soft] **hq-post-parallel-build-verify**: 1. After parallel agents complete, ALWAYS run the project's build and typecheck before reporting success. This is not optional — parallel agents cannot see...
- [soft] **hq-prd-domain-whois-verify-before-dns**: NEVER assume a domain referenced in a PRD is registered or owned by the caller — WHOIS-check every domain before committing ANY of:
- [soft] **hq-prd-verify-passes-vs-artifact-registry**: NEVER: Mark a PRD story `passes: true` when the acceptance criteria says "published to npm at vX.Y.Z" (or "deployed to {url}", "released as {tag}", etc.) bas...
- [soft] **hq-pre-deploy-domain-check**: Before ANY Vercel deploy:
- [soft] **hq-pre-push-gate-probes-prod-not-localhost**: Pre-push E2E gates MUST probe the current production deployment, not a local dev server (localhost, :3000, etc.). Local-env drift — missing secrets, port c...
- [soft] **hq-prefer-agent-browser**: For browser-based QA audits, page-walking, and site testing, use the Vercel agent-browser CLI tool (invoked via `/agent-browser` skill) instead of Claude in ...
- [soft] **hq-prefer-plpgsql-at-time-zone-over-js-port**: When porting timezone-handling logic from JavaScript into PL/pgSQL (triggers, functions, materialized view refreshers), prefer Postgres' native `AT TIME ZONE...
- [soft] **hq-preview-eval-no-navigate-use-click**: When using the `preview_*` MCP tooling for UI verification:
- [soft] **hq-preview-eval-react-input-native-setter**: When setting the value of a React-controlled `<input>` or `<textarea>` through `preview_eval`, use the native prototype setter and dispatch a real input event:
- [soft] **hq-preview-mcp-localhost-origin-bound-cross-origin-curl**: When the Claude Preview MCP server is bound to a local dev server (`localhost:PORT`, typically launched via `rtk init preview`), it cannot navigate to a diff...
- [soft] **hq-preview-screenshot-animation-freeze-before-capture**: Do NOT try to verify sub-second CSS animations (fades, pulses, shimmer sweeps, toast dismissals, etc.) by racing `preview_screenshot` against the animation w...
- [soft] **hq-preview-start-launch-registry-is-global**: `preview_start { name: "..." }` resolves the server name against the user-level Claude Preview launch registry, NOT the repo-local `.claude/launch.json` you ...
- [soft] **hq-preview-start-lsof-port-check**: Before invoking `preview_start { name: "..." }` on a server entry that was just added (or whose port was just changed), probe the target port:
- [soft] **hq-pull-before-work**: ALWAYS run `git pull` (or `git fetch && git merge`) on the active branch before making any changes to a repo. If the repo is significantly behind origin (50+...
- [soft] **hq-react-19-hydration-bail-diagnostics**: When a React 19 app shows the symptom "form submits but does nothing" (button click triggers page navigation or does nothing visible, no API POST in network ...
- [soft] **hq-react-19-render-phase-reset**: When a component needs to reset derived state (clear preview, reset form, abort a previous fetch) because an identifying prop changed, use a render-phase res...
- [soft] **hq-react-effect-callback-ref-stabilize**: ALWAYS: When a `useEffect` runs an animation/timer sequence and depends on a callback prop, stabilize the callback via a ref before using it inside the effect:
- [soft] **hq-redact-structural-plus-regex-pass**: ALWAYS: Structural JSON-field redaction (walking named keys like `apiKey`, `token`, `secret`) MUST be followed by a regex pass on the resulting string values.
- [soft] **hq-rtk-init-preview-hook-command**: ALWAYS preview rtk's hook command via `rtk init --no-patch --hook-only -g` before letting `rtk init` write to project files.
- [soft] **hq-run-project-sigkill-retry**: When a `claude -p` subprocess in `run-project.sh` fails with exit code 137 (SIGKILL), do NOT manually intervene. macOS memory pressure is the most common cau...
- [soft] **hq-rust-helper-extension-audit-call-sites**: ALWAYS audit every existing call site of a buggy helper before declaring the fix done. The default fix is to repair the original helper in place, not to intr...
- [soft] **hq-rust-string-byte-slice-char-boundary-panic**: NEVER write `&s[..n]`, `&s[start..end]`, `s.get(..n)`, or any byte-indexed slice on a Rust string where `n` is not known to land on a UTF-8 char boundary. An...
- [soft] **hq-scanner-no-default-scopes-test-flag**: ALWAYS: Scanner scripts that auto-expand to default parent dirs MUST offer a `--no-default-scopes` (or equivalent) test flag. Without it, fixture tests drag ...
- [soft] **hq-scheduled-task-reference-pattern**: When creating or updating a scheduled task via the scheduled-tasks MCP tools, the system overwrites `SKILL.md` with the `prompt` parameter content. Any detai...
- [soft] **hq-sed-delimiter-not-pipe-on-table-rows**: ALWAYS: When sed-editing markdown table rows (any line containing `|`), use `@` or `#` as the sed substitution delimiter instead of the default `|`. Default ...
- [soft] **hq-session-resume-git-status-reverify**: NEVER trust `git status` output embedded in a session-resume system-reminder as authoritative. The snapshot may pre-date post-commit formatter mutations (pre...
- [soft] **hq-settings-local-hot-reload**: ALWAYS assume Claude Code picks up `.claude/settings.local.json` hook edits WITHOUT a session restart — the next Bash (or other relevant tool) call uses th...
- [soft] **hq-settings-local-wildcard-verb-ask-destructive-shapes**: When broadening a permission verb with a wildcard like `Bash(git:*)` in `.claude/settings.local.json`, always add the destructive shapes for that verb to the...
- [soft] **hq-site-rebuild-asset-audit**: ALWAYS compare total asset count between source site and destination repo when scraping assets for site rebuilds. Do not rely solely on pattern-matching bran...
- [soft] **hq-skill-guard-removal-grep-orphan-refs**: When you remove a skill-level guard, section, or preflight block that is referenced from elsewhere in the file (or related files) by name — e.g. `"same mes...
- [soft] **hq-skill-plugin-pattern**: Integrate external Claude Code skill packages as namespaced plugins using the bridge
- [soft] **hq-slack**: Consolidated Slack rules covering permissions for posting, workspace routing, MCP fallback behavior, and bot token / scope verification. All four sub-rules a... <!-- applies_to: slack -->
- [soft] **hq-smoketest-tracking-via-destination-stats-api**: When you need to verify that a tracking event fired from a webpage actually landed in a downstream analytics / events service (GTM HQ, an internal events API...
- [soft] **hq-social-draft-content-extraction**: Social draft files use this format: <!-- applies_to: social -->
- [soft] **hq-static-regression-anchor-forbidden-pattern**: When a static regression harness (shell `grep`, ripgrep-based test, static analysis rule) forbids an orphan pattern — e.g. a URL path, CLI flag, or API sha...
- [soft] **hq-sub-agent-summary-verify-via-grep**: Never trust a sub-agent's summary of a large config file (>25K Read-token cap) without direct grep verification in the parent session. Before designing, edit...
- [soft] **hq-subagent-granularity-ambiguity**: ALWAYS: Treat "already uses sub-agents" as ambiguous when evaluating context-preservation. Before concluding that an orchestrator preserves parent context, v...
- [soft] **hq-supabase**: Consolidated Supabase guardrails covering project lifecycle, CLI/env alignment, Next.js middleware safety, migration verification, and storage bucket bootstr... <!-- applies_to: supabase -->
- [soft] **hq-svg-recolor-via-brightness-invert-filter**: ALWAYS recolor single-paint SVG logos via CSS `filter: brightness(0) invert(1)` when embedding cross-repo and you need pure white output, regardless of the S...
- [soft] **hq-swarm-rust-hub-files**: Rust projects have "hub files" that nearly every feature story modifies: `Cargo.toml`, `src/lib.rs`, `src/commands/mod.rs`. When running swarm mode on Tauri/...
- [soft] **hq-task-chip-worktree-isolation**: NEVER spawn a Task tool chip that performs git operations in the same worktree another agent or session is actively using. Two chips in one worktree silently...
- [soft] **hq-telegram-single-poller**: Telegram's Bot API allows only ONE active `getUpdates` (long-polling) connection per bot token. A second poller causes 409 Conflict errors and crashes the ne...
- [soft] **hq-tmux-plan-approval-dance**: When `/run-project --tmux` launches a Claude session in tmux, the session requires 3 sequential approvals before execution begins:
- [soft] **hq-toolsearch-load-deferred-schemas**: After a resumed or auto-compacted session, many commonly-used tools (`TodoWrite`, `WebFetch`, `WebSearch`, `AskUserQuestion`, `ExitPlanMode`, MCP tools, etc....
- [soft] **hq-tsup-dts-ship-prebuilt-dist**: NEVER trust `tsup --dts` to produce consistent `.d.ts` output across install environments. When a consumer `npm install`s the library from git, the `prepare`...
- [soft] **hq-tsx-local-bin-over-npx**: For tsx-driven one-off scripts (e.g. `scripts/inspect-*.ts`, `scripts/backfill-*.ts`), ALWAYS prefer:
- [soft] **hq-turbopack-stale-css-reset-dev-cache**: When Turbopack's dev server keeps serving stale CSS after a source edit — especially across `_next/dev/static/chunks/*.css` where new utility classes or ke...
- [soft] **hq-uuid-rfc4122-variant-nibble**: ALWAYS ensure the 4th group (positions 20-23) of a UUID test fixture starts with `8`, `9`, `a`, or `b` when the validator uses an RFC 4122 regex like `[0-9a-...
- [soft] **hq-validator-and-schema-paired-in-pr**: When introducing a validator script that gates a migration across many sibling files (worker manifests, PRD blocks, schema-conformant configs), the PR that a...
- [soft] **hq-vendor-cutover-runbook-default**: Phase-4 vendor cutovers (any migration step that requires touching a vendor dashboard — Vercel project settings, Clerk app config, Stripe webhook URLs, Sup...
- [soft] **hq-vercel**: This file consolidates 31 prior Vercel-related global policies into one topical reference. Rules are grouped by surface area (CLI, deploys, domains, projects... <!-- applies_to: vercel -->
- [soft] **hq-vercel-domain-transfer-reissues-verification**: ALWAYS: When transferring a Vercel domain between projects on the same team (DELETE from project A → POST to project B), expect Vercel to issue a NEW `vc-d... <!-- applies_to: vercel -->
- [soft] **hq-verify-git-after-compact**: ALWAYS: When a session spans a compact boundary, verify any "pending commit" claim in the post-compact summary against the actual git state before redoing wo...
- [soft] **hq-verify-shared-files-after-parallel-agents**: When 3+ parallel agents all edit the same file (e.g., registry.yaml, package.json, INDEX.md), always read the final file after all agents complete and verify...
- [soft] **hq-vitest-conversion-keep-assert-strict**: ALWAYS keep `import { ... } from 'node:assert/strict'` (and all `assert.*` calls in the test body) unchanged when converting a test file between Node's `--te...
- [soft] **hq-vitest-mock-invoke-lazy-reject**: ALWAYS: When mocking `invoke()` from `@tauri-apps/api/core` (or any async function) in vitest error-path tests, use a lazy `mockImplementation` that construc...
- [soft] **hq-webhook-handlers-log-every-200-path**: ALWAYS emit a tagged log line on every code path that returns 200 (or any 2xx) from a webhook handler — including the silent-skip paths. This is non-negoti...
- [soft] **hq-zsh-status-readonly-loop-var**: NEVER use `status` as a local variable name in a shell `for` or `while` loop (or any zsh script context). In zsh, `$status` is a readonly special parameter t...
- [soft] **idb-install**: To install Facebook's `idb` (iOS debug bridge) for iOS simulator control:
- [soft] **nextjs-suspense-dev-fallback**: When a Next.js 16 app with Turbopack dev mode shows RSC pages stuck on their Suspense fallback ("Loading...") despite the server returning 200, do not debug ...
- [soft] **og-image-twitter-cache**: When adding or changing OG images for a site, always deploy and verify the OG image endpoint returns valid PNG before sharing the URL on X/LinkedIn. X caches...
- [soft] **orchestrator-competing-processes**: Before running `--resume` on a project, check for existing `run-project.sh` processes for that project (`ps aux | grep "run-project.*{project}"`). Kill all s...
- [soft] **paginated-api-sort-priority**: When a paginated API serves data that the client filters by status/priority, add server-side `sort` param to return high-priority records first. Client-side ...
- [soft] **paper-flex-column-reorder**: When a Paper artboard uses `display: flex; flex-direction: column` (most page-level artboards do), `write_html insert-children` always appends new nodes at t...
- [soft] **paper-text-width**: When writing HTML via Paper MCP (`write_html`), always set an explicit `width` property on Text nodes (e.g. `width: 260px; text-align: center`). Without expl...
- [soft] **paper-text-wrapping**: Paper MCP text nodes do NOT respect `width` or `max-width` for text wrapping. Text renders as a single unwrapped line regardless of container or node width s...
- [soft] **post-edit-verification**: After completing edits in a repo that has typecheck or lint configured, run the project's verification commands before reporting the task as done:
- [soft] **prd-content-sources**: When creating PRD stories that involve content extraction or analysis of a person's written/spoken work, enumerate ALL available source locations in HQ — n...
- [soft] **prd-files-match-acs-for-swarm**: Before launching `/run-project` with `--swarm`, verify that each story's `files[]` array includes ALL files mentioned in its acceptance criteria. Swarm mode ...
- [soft] **prd-story-sizing**: Every user story in a PRD must have a complexity score computed as:
- [soft] **prd-validation**: Two validation gates protect PRD integrity at different lifecycle points: source-file verification during drafting, and JSON-parse validation after sub-agent...
- [soft] **prd-verify-source-files**: When a PRD story references concrete product strings (button labels, URLs, route paths, success copy, env var names, API field names), open the actual source...
- [soft] **pre-refactor-hygiene**: Before structural refactors on files >300 LOC, first remove dead code in a separate commit:
- [soft] **preview-clerk-auth**: Preview tools (preview_screenshot, preview_snapshot) render blank for apps using Clerk middleware auth. The headless browser has no Clerk session. Verify via...
- [soft] **preview-tool-node-path**: The preview_start tool's shell environment does not have fnm (or nvm) in PATH. Commands like `npm`, `npx`, and bare `node` are not found.
- [soft] **regression-gate-lint-fix**: When a regression gate fails due to lint error increase (e.g. "14 errors, baseline 13, +1 new"), fix the new error directly on the feature branch (checkout, ...
- [soft] **rename-safety-checklist**: When renaming or changing the signature of any function, type, or variable, search separately for all reference categories — a single grep will miss indire...
- [soft] **reread-before-edit-long-sessions**: After 10+ conversation turns, or if context compaction has occurred, re-read any file before editing it. Do not trust cached/remembered file contents — com...
- [soft] **reskin-separate-orchestration-from-visual**: When adapting a reference implementation (another site, app, or codebase) into a new visual direction, explicitly split the adaptation into two columns befor...
- [soft] **run-project-dry-run-branch-leak**: `scripts/run-project.sh --dry-run {slug}` cleans up its ephemeral git worktree when it finishes, but it does NOT delete the feature branch that was created a...
- [soft] **run-project-file-locks-stale**: When `scripts/run-project.sh` finishes a project with all stories passing, the target repo's `.file-locks.json` is often left in a modified-but-empty state �...
- [soft] **run-project-progress-txt-no-commit-misleading**: Lines in `workspace/orchestrator/{slug}/progress.txt` tagged `[no-commit]` do not reliably indicate that a story made no commits. They indicate only that the...
- [soft] **run-project-verification-story-false-negative**: When a PRD story's only deliverable is a verification report (no code commits, no new tests) — e.g. "E2E verification on dev store", "QA smoke test", "code...
- [soft] **session-data-for-product-accuracy**: Before writing user-facing HQ guides or cheatsheets, analyze actual session data in `workspace/threads/` and `workspace/learnings/.observe-patterns*.json` to...
- [soft] **shopify-block-order**: In Shopify JSON templates, the key for ordering blocks is `block_order` (singular), NOT `blocks_order`. <!-- applies_to: shopify -->
- [soft] **shopify-client-credentials**: Shopify no longer issues permanent `shpat_*` Admin API access tokens. Custom apps use Client Credentials Grant (OAuth 2.0): <!-- applies_to: shopify -->
- [soft] **style-pack-first-consumer-scaffold**: When a PRD introduces a cinematic treatment, design language, or motion system that will plausibly be reused by sibling surfaces (other apps within the same ...
- [soft] **subagent-creative-dedup**: When using parallel sub-agents to generate creative content (social posts, marketing copy, reformatted text), always run a dedup pass on the combined output....
- [soft] **swarm-post-execution-review**: After swarm/parallel execution of 5+ stories, ALWAYS:
- [soft] **tauri-event-payload-encoding**: When receiving data from Tauri's JS event system (`app.listen()` + `window.__TAURI__.event.emit()`), the payload arrives double-JSON-encoded. A JSON array `[...
- [soft] **tauri-plugin-setup-async**: In Tauri v2 plugin `setup` callbacks, use `tauri::async_runtime::spawn()` instead of `tokio::spawn()`. The setup closure runs before the Tokio runtime is acc...
- [soft] **verify-routes-after-parallel-execution**: After parallel story execution completes, grep nav components for route hrefs and verify each has a corresponding `page.tsx`. Parallel agents add nav links i...
- [soft] **worker-personality**: When creating or updating worker instructions, apply the negative experience memory pattern
- [soft] **worktree-launch-config**: When using git worktrees for project execution:

