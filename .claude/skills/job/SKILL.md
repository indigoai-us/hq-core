---
name: job
description: Deprecated alias for /schedule — Outpost recurring agent jobs. Prefer /schedule.
allowed-tools: Read, Write, Edit, Grep, Glob, Bash, AskUserQuestion
---

# /job → /schedule (deprecated)

`/job` is a **deprecated alias**. Load and follow `.claude/skills/schedule/SKILL.md`
for all behavior.

When invoked, tell the user once that the command is now `/schedule`, then
execute the schedule skill with the same `$ARGUMENTS`.
