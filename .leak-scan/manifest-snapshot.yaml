# manifest-snapshot.yaml — frozen view of HQ companies/manifest.yaml tenant slugs.
#
# Used by `.leak-scan/scan.sh denylist-drift` to verify the staging denylist
# stays in sync with the HQ manifest. See policy:
#   core/policies/hq-cmd-promote-hq-core-denylist-source-from-manifest.md
#
# Lifecycle:
#   - HQ /promote-hq-core Phase 4 rewrites this file from companies/manifest.yaml
#   - The CI denylist-drift job reads it on every PR
#   - Manual edits get clobbered on next promote
#
# Schema:
#   slugs:        every tenant slug in HQ companies/manifest.yaml
#   public_slugs: subset of `slugs` that are intentionally public-shipped
#                 (brand names that hq-core references in skills, e.g. indigo).
#                 These MUST NOT appear in denylist.companies.
#   private_slugs (informational): slugs - public_slugs. Should appear in
#                 denylist.companies (or in an explicit allowlist exception).

version: 1
generated: 2026-05-04
generator: hq /promote-hq-core (manual seed; auto-rewrite on next run)

slugs:
- liverecover
- indigo
- personal
- golden-thread
- haven-slay
- holler-mgmt
- brandstage
- estate-manager
- keptwork
- tonal
- moonflow
- dominion
- hpo
- amass
- magical-moments
- dripkit
- empire-os
- new-american-codex

public_slugs:
- indigo
- personal  # generic meta-slug, appears in mode docs ("personal/HQ mode") — not a brand leak
